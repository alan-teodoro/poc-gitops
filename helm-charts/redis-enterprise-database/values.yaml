# Default values for redis-enterprise-database
# This is a YAML-formatted file.

# Database identification
database:
  name: cache
  env: dev
  type: cache  # cache, session, persistent

# Cluster reference
cluster:
  name: orders
  team: orders-team
  datacenter: dc1

# Namespace where database will be deployed
# Can be different from cluster namespace for multi-namespace support
# Example: redis-{cluster.name}-dev or redis-{cluster.name}-prod
namespace: redis-orders-dev

# Redis Enterprise Cluster reference
redis:
  # Cluster name to connect to
  clusterName: orders-redis-cluster
  
  # Database configuration
  memorySize: 1GB
  replication: true
  persistence: disabled
  databasePort: 12000
  tlsMode: disabled
  evictionPolicy: volatile-lru
  shardCount: 1
  # Optional mTLS trust list (secret names in database namespace).
  # Each secret must include key: cert
  clientAuthenticationCertificates: []

# Optional GitOps-managed mTLS client-certificate secret.
# Keep disabled by default; enable only for bootstrap/demo databases.
mtls:
  clientCertificateSecret:
    create: false
    # Secret name to create and reference from redis.clientAuthenticationCertificates
    name: ""
    # PEM certificate body stored as stringData.cert
    cert: ""

# Route configuration
route:
  enabled: true
  tlsTermination: passthrough
  # Optional namespace override for Route resource.
  # Use redis-enterprise when DB Service in app namespace is ExternalName.
  namespace: ""
  # Optional custom hostname for Route (for example: cache-dev.example.com)
  host: ""
  # Optional explicit backend service. If empty, defaults to redis-<databasePort>.
  serviceName: ""

# Labels applied to all resources
labels:
  app.kubernetes.io/managed-by: argocd
  gitops: argocd

# Annotations applied to all resources
annotations: {}
